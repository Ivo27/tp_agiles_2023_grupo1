name: test
run-name: Testeando
on: [push]
jobs:
  compilar:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: pwd
      - run: ls
      - run: cd ./tp-ahorcado
      - run: pwd
      - run: npm test
